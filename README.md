# OpenCode Swarm Sandbox

Ephemeral agent workspace. Swarm writes here.

## Swarm Status
The sandbox is ready for agent runs.

## How this works
Modal agents clone this repo, run OpenCode against a Gemini web proxy, and push results.

